# Web migration host

Frontend hiện vẫn nằm tại `D:\warehouse-oms\src` để giữ Vite build và production deployment ổn định.
Các page sẽ được chuyển vào `apps/web/src/features` theo từng vertical slice sau khi compatibility router hoạt động.